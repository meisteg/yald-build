require conf/distro/poky.conf

DISTRO = "yald"
DISTRO_NAME = "yald (Yet Another Linux Distro)"

SDK_VENDOR = "-yaldsdk"
TARGET_VENDOR = "-yald"

DISTRO_FEATURES:remove = "alsa bluetooth ext2 pcmcia 3g nfc x11 opengl ptest wayland vulkan"

SANITY_TESTED_DISTROS:append = " \
    fedora-41 \n \
    "

INHERIT += "image-buildinfo"
